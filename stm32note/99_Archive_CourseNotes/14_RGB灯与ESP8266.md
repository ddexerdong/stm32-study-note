# 14 RGB 灯与 ESP8266

> 覆盖课程：[38] [39-1] [39-2] [39-3] [39-4] [39-5]
>
> 目标：理解 WS2813E 单线 RGB 灯的严格时序、颜色编码、刷新缓冲区和供电共地问题；用 ESP8266 AT 指令建立“串口 WiFi 模块”的学习框架，覆盖基础 AT 测试、STA 模式、AP 模式、透传、发送传感器数据和接收命令控制外设。
>
> 关联章节：[[04_串口与DMA|USART]] · [[06_定时器PWM|TIM / PWM]] · [[10_DWT与单总线传感器|DHT11]] · [[05_ADC模数转换|空气质量 ADC]]

---

## 本节目标

完成本节后，应能做到：

- 把 WS2813E 理解成“按严格时序接收颜色数据的单线 RGB 灯”。
- 知道 RGB 灯调不亮时要先查供电、共地、数据线、时序和颜色顺序。
- 把 ESP8266 理解成“通过 UART 接收 AT 指令的 WiFi 模块”。
- 按基础 AT、模式配置、连接网络、透传、发送数据、接收控制命令的顺序学习 ESP8266。
- 不把未确认的 AT 固件版本、默认波特率、服务器地址、控制协议写成确定事实。

---

## 核心概念

### 知识地图

| 名词 | 工程含义 | 常见位置 |
|------|----------|----------|
| WS2813E | 单线 RGB 灯珠/灯带类器件，按时序接收颜色数据 | `[38]` |
| 单线协议 | 一根数据线同时表达 0、1 和刷新结束 | RGB 灯驱动 |
| 严格时序 | 高低电平宽度必须落在器件允许窗口内 | GPIO / TIM / DMA |
| 颜色编码 | 每颗灯需要发送多个颜色字节 | RGB 缓冲区 |
| 刷新缓冲区 | MCU 内存中保存每颗灯的颜色，再统一刷新 | `BSP/ws2813e.c` |
| Reset / Latch | 发送完成后保持低电平，让灯珠锁存数据 | RGB 时序 |
| 供电注入 | 多颗 RGB 灯电流大，可能需要额外供电 | 硬件调试 |
| ESP8266 | 常见 UART WiFi 模块，可用 AT 指令控制 | `[39-1]` 到 `[39-5]` |
| AT 指令 | 用文本命令配置模块和网络连接 | 串口调试 |
| STA 模式 | ESP8266 作为站点连接路由器 | WiFi 配置 |
| AP 模式 | ESP8266 自己作为热点 | WiFi 配置 |
| 透传 | 串口数据直接透传到网络连接 | 数据发送 |
| 接收命令 | 网络侧发来的文本命令由 STM32 解析后控制外设 | LED / 蜂鸣器 / 舵机 |

### 常用 HAL API

| API / 宏 | 作用 | 常见用法 |
|----------|------|----------|
| `HAL_GPIO_WritePin()` | 控制普通 GPIO 电平 | 简单测试 RGB 数据脚、LED、蜂鸣器 |
| `HAL_TIM_PWM_Start()` | 启动 PWM 输出 | 舵机控制或定时驱动框架 |
| `HAL_UART_Transmit()` | 串口发送 AT 指令或调试文本 | ESP8266 指令发送 |
| `HAL_UART_Receive_IT()` | 中断接收串口数据 | ESP8266 基础接收 |
| `HAL_UARTEx_ReceiveToIdle_DMA()` | 空闲中断 + DMA 接收不定长数据 | ESP8266 响应和网络数据 |
| `__HAL_TIM_SET_COMPARE()` | 修改 PWM 占空比 | 舵机角度或其他输出控制 |

---

## 本质理解

WS2813E 的本质是：**STM32 不是发送普通串口数据，而是在一根数据线上输出严格宽度的高低电平，把颜色位流送进灯珠。**

```text
颜色数组
-> 按灯珠顺序展开成 bit
-> 按 0/1 编码成精确高低电平
-> 输出到 DATA
-> 保持低电平一段时间锁存
```

ESP8266 的本质是：**一个用 UART 控制的 WiFi 调制解调模块。STM32 不直接跑 TCP/IP 细节，而是通过 AT 指令让模块完成联网和收发。**

```text
STM32 UART
-> AT 指令
-> ESP8266
-> WiFi 网络
-> 服务器 / 手机 / 另一个客户端
```

PDF 可以支撑 USART 通信、GPIO 输出、PWM 舵机等基础能力，但当前 PDF 和已有笔记不足以支撑 WS2813E 的纳秒级时序和 ESP8266 的完整 AT 实验细节。所以这一章不写死固件版本、默认波特率、具体服务器地址或远程控制协议。

> 视频待核对：WS2813E 的具体时序窗口、颜色字节顺序、课程驱动方式、灯珠数量、数据引脚、供电方式；ESP8266 的 AT 固件版本、默认波特率、STA/AP/透传指令参数、服务器端配置和控制协议。

以实际开发板原理图和 CubeMX 配置为准。

---

## 工作流程 / 原理

### WS2813E 刷新流程

```text
准备每颗灯的颜色
-> 写入 RGB 缓冲区
-> 按课程确认的颜色顺序编码
-> 用 GPIO / TIM / DMA 输出严格时序
-> 输出 Reset / Latch 低电平
-> 灯珠更新颜色
```

单线 RGB 灯的关键不是 `HAL_GPIO_WritePin()` 会不会用，而是“时间是否足够准”。普通软件延时很容易被中断、编译优化、函数调用开销影响。

> 视频待核对：课程 `[38]` 使用纯 GPIO、定时器 PWM、DMA 还是其他方式生成 WS2813E 时序。

### ESP8266 基础 AT 测试

```text
串口参数确认
-> 发送 AT
-> 等待 OK
-> 查询版本或当前模式
-> 确认 STM32 与 ESP8266 串口收发正常
```

不要一开始就连 WiFi 或透传。先用最短的 AT 指令证明 UART 通了。

> 视频待核对：ESP8266 默认波特率、换行格式、复位等待时间和课程使用的串口工具测试步骤。

### STA 模式框架

```text
设置 STA 模式
-> 连接路由器
-> 查询 IP
-> 建立网络连接
-> 进入普通收发或透传
```

WiFi 名称、密码、服务器地址、端口都不写真实值，只用占位符。具体指令参数：

> 视频待核对：课程 `[39-2]` 的 STA 模式 AT 指令顺序、是否开启单连接/多连接、是否使用透传。

### AP 模式框架

```text
设置 AP 模式
-> 配置热点名称和密码
-> 手机连接热点
-> 建立连接
-> 进行串口和网络透传测试
```

> 视频待核对：课程 `[39-3]` 的 AP 模式热点配置、手机端工具、端口和数据收发方式。

### 发送传感器数据

```text
读取 DHT11 / 空气质量等数据
-> 格式化为文本或协议帧
-> 通过 ESP8266 发送
-> 网络端确认收到
```

发送格式可以是简单文本，也可以是自定义协议或 HTTP/MQTT 等，但当前资料不足以确定课程使用哪一种。

> 视频待核对：课程 `[39-4]` 的传感器数据格式、发送周期、网络协议和接收端工具。

### 接收命令控制外设

```text
ESP8266 收到网络数据
-> STM32 串口接收
-> 解析命令
-> 控制蜂鸣器 / LED / 舵机
-> 返回执行结果
```

这部分最容易把“通信层”和“业务层”混在一起。先把收到的原始字符串打印出来，再做命令解析。

> 视频待核对：课程 `[39-5]` 的命令格式、LED/蜂鸣器/舵机引脚、舵机 PWM 通道和反馈内容。

---

## CubeMX / MDK 配置

### WS2813E 配置思路

1. 选择当前 STM32F103 工程或新建工程。
2. 配置系统时钟，确保时序基准稳定。
3. 按课程确认的数据输出方式配置 GPIO 或 TIM。
4. 如果使用 GPIO 位带或直接寄存器方式，数据引脚配置为推挽输出。
5. 如果使用 TIM/PWM/DMA 方式，配置对应定时器通道和 DMA。
6. 基础调试阶段先点亮少量灯珠，减少供电和缓冲区变量。
7. 生成 MDK 工程。
8. 用户代码写在 `USER CODE` 区域；RGB 驱动建议放在 `BSP/ws2813e.c` 和 `BSP/ws2813e.h`。

WS2813E 数据引脚、灯珠数量、颜色顺序、时序参数、驱动方式：

以实际开发板原理图和 CubeMX 配置为准。

### ESP8266 配置思路

1. 确认 ESP8266 模块供电能力，避免只看串口不看电源。
2. 在 CubeMX 中打开课程使用的 USART。
3. 配置 TX/RX 引脚，确认 STM32 TX 接 ESP8266 RX，STM32 RX 接 ESP8266 TX。
4. 配置波特率、数据位、停止位、校验位；具体以课程和模块固件为准。
5. 基础 AT 测试先用阻塞收发或中断接收。
6. 不定长响应和透传数据建议后续接入空闲中断 + DMA。
7. 如果要控制舵机，额外配置 TIM PWM；LED/蜂鸣器配置 GPIO 输出。
8. 生成 MDK 工程。
9. `BSP/esp8266.c` 只封装 AT 发送、等待响应、接收解析，不直接写复杂业务。

ESP8266 波特率、串口编号、复位脚、使能脚、STA/AP/透传参数：

以实际开发板原理图和 CubeMX 配置为准。

---

## 最小代码

以下代码都是示例框架，不声称可直接编译运行。WS2813E 精确时序、ESP8266 AT 指令参数和接收状态机需要按课程视频、模块资料和实际工程补齐。

### `BSP/ws2813e.h`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
#ifndef WS2813E_H
#define WS2813E_H

#include "main.h"

void WS2813E_SetPixel(uint16_t index, uint8_t red, uint8_t green, uint8_t blue);
void WS2813E_Clear(void);
void WS2813E_Refresh(void);

#endif
```

### `BSP/ws2813e.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
#include "ws2813e.h"

#define WS2813E_LED_COUNT  8

typedef struct
{
    uint8_t red;
    uint8_t green;
    uint8_t blue;
} WS2813E_Color;

static WS2813E_Color ws2813e_buf[WS2813E_LED_COUNT];

void WS2813E_SetPixel(uint16_t index, uint8_t red, uint8_t green, uint8_t blue)
{
    if (index >= WS2813E_LED_COUNT)
    {
        return;
    }

    ws2813e_buf[index].red = red;
    ws2813e_buf[index].green = green;
    ws2813e_buf[index].blue = blue;
}

void WS2813E_Clear(void)
{
    uint16_t i;

    for (i = 0; i < WS2813E_LED_COUNT; i++)
    {
        WS2813E_SetPixel(i, 0, 0, 0);
    }
}

void WS2813E_Refresh(void)
{
    /*
     * 这里不能用普通串口方式发送。
     * 需要按课程确认的 WS2813E 时序，把 ws2813e_buf 编码成单线波形。
     * 具体可由 GPIO 精确延时、TIM PWM、DMA 等方式实现。
     */
}
```

### `BSP/esp8266.h`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
#ifndef ESP8266_H
#define ESP8266_H

#include "main.h"

HAL_StatusTypeDef ESP8266_SendLine(const char *cmd);
HAL_StatusTypeDef ESP8266_BasicTest(void);
HAL_StatusTypeDef ESP8266_EnterStaMode(const char *ssid, const char *password);
HAL_StatusTypeDef ESP8266_SendPayload(const char *payload);
void ESP8266_HandleRxLine(const char *line);

#endif
```

### `BSP/esp8266.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
#include "esp8266.h"
#include "usart.h"
#include <string.h>

#define ESP8266_UART_HANDLE huart1

HAL_StatusTypeDef ESP8266_SendLine(const char *cmd)
{
    HAL_StatusTypeDef ret;

    ret = HAL_UART_Transmit(&ESP8266_UART_HANDLE,
                            (uint8_t *)cmd,
                            (uint16_t)strlen(cmd),
                            1000);
    if (ret != HAL_OK)
    {
        return ret;
    }

    return HAL_UART_Transmit(&ESP8266_UART_HANDLE, (uint8_t *)"\r\n", 2, 1000);
}

HAL_StatusTypeDef ESP8266_BasicTest(void)
{
    return ESP8266_SendLine("AT");
}

HAL_StatusTypeDef ESP8266_EnterStaMode(const char *ssid, const char *password)
{
    /*
     * 这里不要写真实 WiFi 名称和密码。
     * AT 指令细节需要按课程视频和 ESP8266 固件版本核对。
     */
    (void)ssid;
    (void)password;

    return ESP8266_SendLine("AT+CWMODE=<sta_mode>");
}

HAL_StatusTypeDef ESP8266_SendPayload(const char *payload)
{
    /*
     * 透传、普通发送、TCP/UDP/HTTP/MQTT 的发送方式不同。
     * 这里先保留统一出口，具体协议后续补齐。
     */
    return HAL_UART_Transmit(&ESP8266_UART_HANDLE,
                             (uint8_t *)payload,
                             (uint16_t)strlen(payload),
                             1000);
}

void ESP8266_HandleRxLine(const char *line)
{
    /*
     * 先打印或观察原始 line。
     * 再按课程确认的命令格式控制 LED、蜂鸣器或舵机。
     */
    (void)line;
}
```

### `Core/Src/main.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
#include "main.h"
#include "usart.h"
#include "gpio.h"
#include "esp8266.h"
#include "ws2813e.h"

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_USART1_UART_Init();

    WS2813E_SetPixel(0, 20, 0, 0);
    WS2813E_Refresh();

    (void)ESP8266_BasicTest();

    /*
     * 示例占位符，不要写真实 WiFi 名称、密码或服务器地址。
     * ESP8266_EnterStaMode("<ssid>", "<password>");
     * ESP8266_SendPayload("temperature=25,humidity=60");
     */

    while (1)
    {
    }
}
```

---

## 调试方法

### 供电

- WS2813E 灯珠电流可能远大于普通 LED，先确认电源能力。
- ESP8266 发射 WiFi 时电流会突然增大，供电不足会重启、乱码或无响应。
- 不确定模块电压等级时，不要直接接 5V。

### 共地

- STM32、WS2813E 外部电源、ESP8266、USB 串口工具必须共地。
- RGB 灯外部供电但没有共地时，数据线电平没有共同参考。
- ESP8266 串口转接工具和 STM32 共用时，也要确认地线连接。

### 接线

- WS2813E 的数据方向要接输入端，不要接到灯带输出端。
- ESP8266 的 TX/RX 要交叉连接。
- 如果模块有 EN、RST、BOOT 等控制脚，状态以实际模块资料为准。
- RGB 数据引脚、ESP8266 串口号、复位脚、舵机 PWM 通道：以实际开发板原理图和 CubeMX 配置为准。

### CubeMX 配置

- WS2813E 使用 GPIO、TIM、DMA 哪种方式，要和驱动代码一致。
- ESP8266 的 USART 参数必须和模块固件一致。
- 接收不定长数据时，确认中断或 DMA 已启动。
- 舵机控制命令如果涉及 PWM，先复用 `06_定时器PWM.md` 的检查方法。

### HAL API 返回值

- ESP8266 发送 AT 指令时先检查 `HAL_UART_Transmit()` 返回值。
- 使用中断接收时，确认 `HAL_UART_Receive_IT()` 或空闲 DMA 接收函数已启动。
- WS2813E 如果用 TIM/DMA，检查 `HAL_TIM_PWM_Start_DMA()` 或相关启动函数返回值。

### 逻辑分析仪 / 串口打印

- WS2813E 用逻辑分析仪看数据线高低电平宽度和 Reset 低电平。
- ESP8266 先用串口打印原始响应，不要直接进入命令解析。
- 透传阶段打印收发长度和原始字符串，避免把网络问题误判成业务问题。

### 外设协议

- WS2813E 的 0/1 编码、颜色顺序、Reset 时间必须按器件和课程确认。
- ESP8266 AT 指令通常要求特定换行、等待时间和响应判断。
- STA、AP、透传不是一个概念，先确认当前模块处在哪种模式。

### 业务逻辑

- RGB 颜色不对，先核对颜色顺序和缓冲区索引。
- ESP8266 能回 `OK` 但不能联网，优先看模式、SSID/密码、路由器频段和连接状态。
- 接收命令能打印但不能控制外设，再查命令解析、GPIO/PWM 输出和业务分支。

---

## 工程意义

WS2813E 代表“对时序要求很高的单线外设”。学会它以后，我会更重视 MCU 指令执行时间、中断影响、定时器/DMA 生成波形这些问题，而不是只依赖普通延时。

ESP8266 代表“用串口接入网络能力”的模块。真实项目里，STM32 常常不直接处理复杂网络协议，而是通过通信模块把传感器数据发出去，再把远程命令收回来控制本地执行器。

---

## 易混淆点

| 容易混淆 | 正确理解 |
|----------|----------|
| RGB 灯就是三个普通 PWM LED | WS2813E 是带控制芯片的单线串行 RGB 灯，重点是时序编码 |
| 颜色值写进数组就会显示 | 还必须调用刷新函数，把缓冲区按时序发出去 |
| 普通 `HAL_Delay()` 可以生成纳秒级时序 | 毫秒延时不适合生成 WS2813E 位时序 |
| ESP8266 是 STM32 的串口调试助手 | 它是 WiFi 模块，串口只是控制入口 |
| `AT` 返回 OK 就等于联网成功 | OK 只说明基础串口和 AT 解析正常 |
| STA、AP、透传是一回事 | STA 是连接路由器，AP 是自己开热点，透传是数据转发方式 |
| 接收控制命令必须一上来写完整协议 | 先打印原始数据，再做最小命令解析 |

---

## 我的理解

我现在把 WS2813E 理解成“一个特别挑时间的移位寄存器灯带”。它不是普通 LED，亮不亮很多时候不是颜色值错，而是供电、共地、数据方向或时序不对。

我现在把 ESP8266 理解成“串口后面接了一个 WiFi 世界”。STM32 先把 AT 指令发清楚，再等模块明确回复，最后才把传感器数据或控制命令放到网络收发里。

以后调 ESP8266，我会先只发 `AT` 看 `OK`，再查模式，再联网，再透传，最后才写业务协议。不要在串口还没稳定时就同时调 WiFi、服务器和 LED/舵机控制。

---

## [38] WS2813E RGB灯应用

> 来源：PDF 弱对应 + 视频待核对 + 模块资料待核对

> 视频待核对：WS2813E 纳秒级时序、颜色编码顺序、灯珠数量、复位/锁存时间、驱动方式和供电方案。

### 实验定位

WS2813E 是严格时序类单线 RGB 灯实验。PDF 的 GPIO/TIM/DMA 章节只能支撑底层思路，不能凭空补完整时序。

### 工作框架

```text
准备颜色缓冲区
-> 把 RGB 数据编码成单线时序波形
-> 通过 GPIO/TIM/DMA 等方式输出
-> 等待 reset/latch
-> 更新下一帧颜色
```

### 最小框架

常见位置：`BSP/ws2813e.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
typedef struct
{
    uint8_t r;
    uint8_t g;
    uint8_t b;
} WS2813E_Color_t;

void WS2813E_SetPixel(uint16_t index, WS2813E_Color_t color)
{
    /* 视频待核对：灯珠数量和颜色顺序。 */
    (void)index;
    (void)color;
}

void WS2813E_Refresh(void)
{
    /* 视频待核对：编码时序、输出方式和 reset/latch 时间。 */
}
```

### 调试方法

- 先确认灯带供电能力和共地。
- 只点亮第一个灯珠，确认时序和颜色顺序。
- 出现随机颜色时，优先查时序、电平和供电。

### 常见坑

- 没共地。
- 供电电流不足。
- 具体纳秒时序未核对就手写延时。
- RGB/GRB 顺序猜错。

## [39-1] ESP8266基础AT指令测试

> 来源：PDF 弱对应 + 视频待核对 + 模块资料待核对

> 视频待核对：ESP8266 固件版本、默认波特率、AT 指令结尾、响应格式和串口连接方式。

### 实验定位

PDF USART 章节只能支撑串口收发。ESP8266 AT 固件和响应流程必须按视频或 AT 文档确认。

### 基础状态机

```text
上电复位
-> 串口发送 AT
-> 等待 OK
-> 查询版本或模式
-> 根据返回值决定下一步
```

### 最小框架

常见位置：`BSP/esp8266.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
uint8_t ESP8266_SendCommand(const char *cmd, const char *expect, uint32_t timeout_ms)
{
    /* 视频待核对：指令结尾、接收缓冲区和响应匹配方式。 */
    (void)cmd;
    (void)expect;
    (void)timeout_ms;
    return 0;
}
```

### 常见坑

- AT 指令换行错误。
- 波特率不匹配。
- 模块供电不足导致反复复位。

## [39-2] ESP8266_STA模式连接WIFI与透传

> 来源：PDF 弱对应 + 视频待核对 + 模块资料待核对

> 视频待核对：STA 模式 AT 指令顺序、WiFi 参数写法、连接返回值、透传进入/退出步骤和课程测试工具。

### 实验定位

不能写真实 WiFi 名称、密码、服务器 IP 或端口。这里仅保留状态机框架。

### 保守流程

```text
AT 基础测试
-> 设置 STA 模式
-> 连接 WiFi
-> 建立 TCP/UDP 连接
-> 进入透传
-> 发送数据
-> 退出透传
```

### 最小框架

常见位置：`BSP/esp8266.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
uint8_t ESP8266_EnterStaPassthrough(void)
{
    /* 视频待核对：AT 指令顺序、参数、响应和超时。 */
    return 0;
}
```

### 常见坑

- 把 AT 指令测试成功当成已经联网成功。
- 透传模式下仍按普通 AT 响应解析。
- 没有设计退出透传的路径。

## [39-3] ESP8266_AP模式_手机连接热点与透传

> 来源：PDF 弱对应 + 视频待核对 + 模块资料待核对

> 视频待核对：AP 模式配置、热点名称/密码规则、手机端工具、连接端口和透传步骤。

### 保守流程

```text
AT 基础测试
-> 设置 AP 模式
-> 配置热点参数
-> 手机连接热点
-> 建立连接
-> 进入透传
-> 双向发送测试
```

### 最小框架

常见位置：`BSP/esp8266.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
uint8_t ESP8266_EnterApPassthrough(void)
{
    /* 视频待核对：AP 参数、服务端/客户端角色和连接流程。 */
    return 0;
}
```

### 常见坑

- AP 模式和 STA 模式角色混淆。
- 手机连上热点但没有建立 TCP/UDP 连接。
- 不区分 AT 模式和透传模式。

## [39-4] ESP8266发送DHT11_空气质量数据实验

> 来源：PDF 弱对应 + 视频待核对 + 模块资料待核对

> 视频待核对：DHT11/空气质量数据格式、发送周期、网络协议、服务器或手机端接收方式。

### 保守流程

```text
读取 DHT11
-> 读取空气质量 ADC 或模块值
-> 格式化应用层数据
-> ESP8266 建立连接
-> 发送数据
-> 等待发送结果
```

### 最小框架

常见位置：`Core/Src/main.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
char payload[64];

void App_SendSensorData(void)
{
    /* 视频待核对：字段格式、发送周期和协议。 */
    snprintf(payload, sizeof(payload), "T=%d,H=%d,AIR=%u",
             temperature, humidity, air_raw);
    ESP8266_SendData(payload);
}
```

### 常见坑

- 传感器读取失败仍发送旧数据。
- 网络发送阻塞影响采样节奏。
- 数据格式没有和接收端约定。

## [39-5] ESP8266接收控制蜂鸣器_LED灯_舵机实验

> 来源：PDF 弱对应 + 视频待核对 + 模块资料待核对

> 视频待核对：远程控制命令格式、透传接收方式、LED/蜂鸣器有效电平、舵机 PWM 通道和业务协议。

### 保守流程

```text
ESP8266 接收网络数据
-> 串口缓冲区得到命令
-> 判断命令类型
-> 分发到 LED / 蜂鸣器 / 舵机
-> 返回执行结果
```

### 最小框架

常见位置：`BSP/esp8266_app.c`

> 代码性质：示例框架，用于理解流程，不能保证直接编译。

```c
void Esp8266_DispatchCommand(const char *cmd)
{
    if (cmd == NULL)
    {
        return;
    }

    /* 视频待核对：命令格式、参数和返回协议。 */
}
```

### 常见坑

- 网络协议没定，就直接写业务判断。
- 串口接收不定长数据没有帧结束条件。
- 舵机 PWM 通道和 LED/蜂鸣器 GPIO 没按 CubeMX 核对。

---

## 本章仍需视频核对

- [ ] WS2813E 的具体时序窗口、颜色字节顺序、课程驱动方式、灯珠数量、数据引脚、供电方式；ESP8266 的 AT 固件版本、默认波特率、STA/AP/透传指令参数、服务器端配置和控制协议。
- [ ] 课程 `[38]` 使用纯 GPIO、定时器 PWM、DMA 还是其他方式生成 WS2813E 时序。
- [ ] ESP8266 默认波特率、换行格式、复位等待时间和课程使用的串口工具测试步骤。
- [ ] 课程 `[39-2]` 的 STA 模式 AT 指令顺序、是否开启单连接/多连接、是否使用透传。
- [ ] 课程 `[39-3]` 的 AP 模式热点配置、手机端工具、端口和数据收发方式。
- [ ] 课程 `[39-4]` 的传感器数据格式、发送周期、网络协议和接收端工具。
- [ ] 课程 `[39-5]` 的命令格式、LED/蜂鸣器/舵机引脚、舵机 PWM 通道和反馈内容。
- [ ] WS2813E 纳秒级时序、颜色编码顺序、灯珠数量、复位/锁存时间、驱动方式和供电方案。
- [ ] ESP8266 固件版本、默认波特率、AT 指令结尾、响应格式和串口连接方式。
- [ ] STA 模式 AT 指令顺序、WiFi 参数写法、连接返回值、透传进入/退出步骤和课程测试工具。
- [ ] AP 模式配置、热点名称/密码规则、手机端工具、连接端口和透传步骤。
- [ ] DHT11/空气质量数据格式、发送周期、网络协议、服务器或手机端接收方式。
- [ ] 远程控制命令格式、透传接收方式、LED/蜂鸣器有效电平、舵机 PWM 通道和业务协议。

---
## 复习检查清单

- [ ] 能说明 WS2813E 为什么不能按普通 UART 发送数据。
- [ ] 能说出 RGB 灯调试时供电和共地为什么优先级最高。
- [ ] 能解释颜色缓冲区和刷新函数的关系。
- [ ] 能说明 WS2813E 时序、颜色顺序和驱动方式需要视频核对。
- [ ] 能说出 ESP8266 的基础 AT 测试流程。
- [ ] 能区分 STA 模式、AP 模式和透传。
- [ ] 能写出“传感器数据 -> ESP8266 发送”的框架。
- [ ] 能写出“ESP8266 接收命令 -> STM32 控制 LED/蜂鸣器/舵机”的框架。
- [ ] 能按“供电 -> 共地 -> 接线 -> CubeMX -> HAL 返回值 -> 波形/串口 -> 协议 -> 业务逻辑”排查问题。
