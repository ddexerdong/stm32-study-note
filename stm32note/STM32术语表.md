# STM32 术语表

> 面向 STM32F103 / HAL / CubeMX 学习路径的高频术语速查。
>
> 目标不是替代数据手册和参考手册，而是快速判断一个词“属于哪类问题、该去哪个章节复习、工程上要注意什么”。

---

## 使用方法

- 先看术语所属类别，再回到对应章节复习。
- 这份表偏概念和工程理解，具体寄存器位以官方手册为准。
- 同名概念在不同 STM32 系列上可能有细节差异，本表默认 STM32F103C8T6。

---

## 基础概念

| 术语 | 工程理解 | 章节 |
|------|----------|------|
| STM32 | ST 公司基于 ARM 内核设计的微控制器系列 | 01 |
| MCU | Microcontroller Unit，单片机，CPU + 存储器 + 外设 | 01 |
| ARM Cortex-M3 | STM32F103 使用的 CPU 内核 | 01 |
| HAL | ST 提供的硬件抽象层，用函数封装寄存器操作 | 01 |
| CubeMX | 图形化配置芯片、引脚、时钟和外设的工具 | 01 |
| CubeIDE | 集成 CubeMX、GCC、GDB 的开发环境 | 01 |
| 数据手册 | 查引脚、电气特性、封装和复用功能 | 01 |
| 参考手册 | 查外设原理、寄存器和时钟细节 | 01 |
| 寄存器 | 芯片内部固定地址的特殊存储单元 | 01 |
| 内存映射 | 把外设寄存器映射到 CPU 可访问的地址空间 | 01 |
| 片上外设 | MCU 内部自带模块，如 GPIO、USART、ADC、TIM | 01 |

---

## GPIO 与硬件接口

| 术语 | 工程理解 | 章节 |
|------|----------|------|
| GPIO | 通用输入输出引脚 | 02 |
| Port | 一组 GPIO，例如 GPIOA、GPIOB | 02 |
| Pin | 端口中的某一脚，例如 PA0、PB3 | 02 |
| Pinout | 芯片引脚功能分布图 | 01 |
| 推挽输出 | 主动输出高/低电平 | 02 |
| 开漏输出 | 只能主动拉低，高电平靠上拉 | 02 |
| 上拉输入 | 悬空默认为高电平 | 02 |
| 下拉输入 | 悬空默认为低电平 | 02 |
| 浮空输入 | 无上下拉，悬空时电平不确定 | 02 |
| 模拟输入 | 关闭数字输入逻辑，供 ADC 使用 | 05 |
| 复用推挽 | GPIO 交给外设输出，如 UART TX、PWM | 02, 04, 06 |
| 复用开漏 | GPIO 交给外设开漏输出，如 I2C | 02 |
| 输出速度 | 控制边沿快慢，不是程序运行速度 | 02 |
| 共地 | 多个模块必须共享 GND 作为信号参考 | 02, 04 |
| 限流电阻 | 限制 LED 等负载电流，保护 GPIO | 02 |
| 重映射 | 把外设功能切换到另一组可用引脚 | 04 |

---

## 时钟与定时器

| 术语 | 工程理解 | 章节 |
|------|----------|------|
| 时钟树 | 芯片内部时钟来源、倍频、分频关系 | 01 |
| HSE | 外部高速时钟，常见核心板为 8 MHz 晶振 | 01 |
| HSI | 内部高速 RC 时钟 | 01 |
| PLL | 倍频器，把低频时钟提升到高频 | 01 |
| SYSCLK | 系统主时钟 | 01 |
| AHB | 高速总线，连接 CPU、Flash、SRAM、DMA 等 | 01 |
| APB1 | 低速外设总线，挂 TIM2/3/4、USART2/3、I2C 等 | 01, 03 |
| APB2 | 高速外设总线，挂 GPIO、AFIO、USART1、ADC、TIM1 等 | 01, 03 |
| PCLK | APB 总线时钟 | 03 |
| TIMx_CLK | 定时器实际计数时钟 | 03, 06 |
| PSC | 定时器预分频值 | 03, 06 |
| ARR | 自动重装载值，决定计数周期 | 03, 06 |
| CNT | 当前计数值 | 03, 06 |
| CCR | 比较值，决定 PWM 高电平时间 | 06 |
| 输入捕获 | 外部边沿到来时，把当前 CNT 保存到 CCR | 08 |
| 编码器模式 | TIM 根据 A/B 相脉冲自动加减计数 | 08 |
| A/B 相 | 编码器两路相差约 90 度的方波 | 08 |
| Update Event | 定时器溢出产生的更新事件 | 03, 07 |
| TRGO | 定时器触发输出，可触发 ADC 等外设 | 07 |
| PWM | 固定频率下改变占空比的输出波形 | 06 |
| 占空比 | 一个周期内高电平占比 | 06 |
| 互补输出 | 高级定时器输出一对反相信号 | 06 |
| 死区 | 互补输出切换时的安全空档 | 06 |
| 刹车输入 | 故障时硬件强制关闭 PWM | 06 |
| TRIG | 超声波模块触发脚，由 MCU 输出 10 us 高电平 | 08 |
| ECHO | 超声波模块回响脚，高电平宽度表示声波往返时间 | 08 |
| 脉宽测量 | 测量一个高电平或低电平持续时间 | 08 |

---

## 中断与事件

| 术语 | 工程理解 | 章节 |
|------|----------|------|
| 中断 | 硬件事件让 CPU 暂停当前任务先处理紧急逻辑 | 03 |
| ISR | 中断服务函数，真正的中断入口 | 03 |
| Callback | HAL 调给用户实现的回调函数 | 03, 04, 05, 07 |
| NVIC | Cortex-M 中断控制器 | 03 |
| 抢占优先级 | 决定能不能打断其他中断 | 03 |
| 响应优先级 | 同抢占优先级同时到来时的排队顺序 | 03 |
| SysTick | 内核定时器，HAL 默认用作 1 ms 时基 | 01, 03 |
| EXTI | GPIO 外部中断线 | 03 |
| FLAG | 状态标志位，表示某事件发生 | 03, 04, 07 |
| `volatile` | 防止中断/硬件共享变量被编译器错误优化 | 03 |
| 阻塞 | 函数不返回，CPU 等待 | 03, 04, 05 |
| 非阻塞 | 函数立即返回，后续靠中断、DMA 或状态机处理 | 03, 04, 05 |

---

## 串口与 DMA

| 术语 | 工程理解 | 章节 |
|------|----------|------|
| UART | 异步串口通信外设 | 04 |
| USART | 支持同步能力的串口，常按 UART 使用 | 04 |
| TX | 发送脚 | 04 |
| RX | 接收脚 | 04 |
| 波特率 | 串口每秒传输的码元数 | 04 |
| 8N1 | 8 数据位、无校验、1 停止位 | 04 |
| TTL 电平 | STM32 常用 3.3 V 逻辑电平 | 04 |
| 交叉连接 | 自己 TX 接对方 RX，自己 RX 接对方 TX | 04 |
| IDLE | 串口空闲线状态，可用于判断一帧结束 | 04 |
| DMA | 硬件自动搬数据，减少 CPU 字节级搬运 | 04, 05 |
| NDTR | DMA 剩余传输计数器 | 04 |
| Circular | DMA 循环模式 | 04, 05 |
| Normal | DMA 搬完一次后停止 | 04, 05 |
| Buffer | 接收或发送缓冲区 | 04, 05 |
| ASCII | 字符和字节之间的基础编码规则 | 04 |

---

## C 类型与数据宽度

这些类型都表示**无符号整数**，区别是占用的位数不同。嵌入式里优先使用这类固定宽度类型，因为它们在不同编译器和平台上含义更明确。

| 类型 | 位数 | 范围 | 常用场景 |
|------|------|------|----------|
| `uint8_t` | 8 位 | 0~255 | 字节、串口数据、数组缓冲区、DMA 字节流 |
| `uint16_t` | 16 位 | 0~65535 | ADC 值、16 位定时器计数值、16 位寄存器值 |
| `uint32_t` | 32 位 | 0~4294967295 | 系统时间、较大计数、定时器时间差、HAL API 参数 |

工程注意：

- `uint8_t` 常用于串口收发，因为串口一次传输的基本单位是 1 字节。
- `uint16_t` 能覆盖 STM32F103 的 12 位 ADC 值和多数 16 位定时器计数值。
- `uint32_t` 常用于 `HAL_GetTick()` 返回值和时间差计算，约 49.7 天后会溢出回 0。
- 计算时间差时建议使用 `HAL_GetTick() - start >= delay`，不要直接比较绝对时间点，这样能兼容 `uint32_t` 溢出。

---

## ADC

| 术语 | 工程理解 | 章节 |
|------|----------|------|
| ADC | 把模拟电压转换成数字值 | 05 |
| 分辨率 | 数字量能分成多少级，F103 常见为 12 位 | 05 |
| LSB | 最小量化单位 | 05 |
| VREF | ADC 参考电压 | 05 |
| 采样时间 | 输入电压给采样电容充电的时间 | 05 |
| 校准 | 上电后补偿 ADC 内部偏差 | 05, 07 |
| 规则组 | 常规 ADC 采样序列 | 05 |
| 注入组 | 高优先级 ADC 采样序列 | 05 |
| 扫描模式 | 多通道按 Rank 顺序采样 | 05 |
| Rank | 多通道采样顺序 | 05 |
| 外部触发 | 由 TIM 等外设触发 ADC 转换 | 07 |
| 连续转换 | 一轮结束后自动继续下一轮 | 05 |
| ADC 值 | ADC 输出的数字值，常见 0~4095 | 05 |
| 电压换算 | 把 ADC 值按 VREF 转为实际电压 | 05 |

---

## 高频判断表

| 看到这个词 | 先想什么 |
|------------|----------|
| `PSC / ARR` | 定时器周期怎么算 |
| `CCR` | PWM 高电平持续多久 |
| `CNT` | 当前计数值是多少 |
| `TRGO` | 哪个定时器在触发哪个外设 |
| `ECHO` | 高电平宽度对应多长时间 |
| `Rank` | ADC 多通道数据对应数组哪个下标 |
| `Circular` | 缓冲区满了是否从头覆盖 |
| `IDLE` | 串口一帧是否结束 |
| `EXTI` | GPIO 电平变化是否触发中断 |
| `volatile` | 变量是否在中断和主循环之间共享 |
| `DMA` | 数据是否让硬件自动搬 |
| `VREF` | ADC 最大参考电压是多少 |

---

## 章节对照

| 章节 | 关键词 |
|------|--------|
| 01 准备与基础认知 | STM32、ARM、寄存器、手册、CubeMX、时钟树 |
| 02 GPIO 与传感器 | GPIO、推挽、开漏、上拉、按键、LM393 |
| 03 中断与定时器 | NVIC、EXTI、SysTick、TIM、PSC、ARR |
| 04 串口与 DMA | USART、TX/RX、printf、IDLE、DMA、ASCII |
| 05 ADC 模数转换 | ADC、采样时间、校准、Rank、DMA、传感器换算 |
| 06 定时器 PWM | PWM、CCR、占空比、舵机、呼吸灯、TIM1 |
| 07 TIM 触发 ADC | TRGO、Update Event、外部触发、内部温度 |
| 08 TIM 通道捕获 | 输入捕获、编码器模式、A/B 相、TRIG、ECHO、脉宽测量 |

---

## 我的理解

术语表不是用来背的，是用来快速定位问题的。

看到一个词，我先判断它属于：

```text
硬件连接
-> 时钟/定时
-> GPIO 模式
-> 中断事件
-> 数据搬运
-> 模拟采样
-> 通信协议
```

分类清楚后，再回到对应章节看 CubeMX 配置、HAL API 和常见错误，效率会高很多。
